# ТОП СКРИПТ — Полное руководство

## Что делает скрипт
Автоматизирует полный Git-воркфлоу: **обновление от препода → синхронизация → коммит своих изменений → пуш**. Заменяет ручной ZIP + копипаст + `git add/commit/push`.

## Что нужно сделать перед работой скрипта
- Так как у насс ессть в репе только одна ветка, нам нужно добавить ветку препода, соответсвенно вот инструцкция: 
```bash
Проверь текущие remotes: git remote -v (должен быть origin — твой репозиторий).

Добавь upstream препода: git remote add upstream https://github.com/ПРЕПОД/РЕПО_ПРЕПОДА.git (замени на ссылку репы преподователя, НОООО!!! не ссылку на сайт по типу гитхаба или флика, именно на репозиторий).

Загрузи изменения: git fetch upstream.

Переключись на main (или master): git checkout main.

Влей обновления: git pull upstream main (или git merge upstream/main, если конфликты).

```





## 🔍 Пошаговое объяснение

### 1. Обновление от препода (upstream)
```bash
git fetch upstream           # Скачивает свежие коммиты препода БЕЗ слияния
git checkout main           # Переключается на основную ветку
git merge upstream/main    # Вливает все новые файлы/изменения от препода
```
- Результат: Твой репозиторий содержит все файлы препода
## 2. Синхронизация с GitHub (origin)
```bash
git pull origin main      # Подтягивает твои последние изменения с GitHub
```
## 3. Проверка изменений
```bash
git status                 # Показывает изменённые/новые файлы
if git diff --quiet; then # Если нет изменений — завершаем
    echo "Нет изменений для коммита. Готово!"
    exit 0
fi
```
## 4. Подготовка к коммиту
```bash
git add .                 # Добавляет ВСЕ изменённые/новые файлы
read -p "Введите сообщение: " commit_message  # Спрашивает сообщение
if [ -z "$commit_message" ]; then exit; fi    # Enter = отмена

```

## 5. Финал УЖЕЕ
```bash

git commit -m "$commit_message"  # Создаёт коммит
git push origin main            # Отправляет на GitHub

```

## САМ ПРИЦИП РАБОТЫЫ
```text
📦 Репо препода → 🔄 fetch/merge upstream → 📥 pull origin → ✏️ твои изменения? → 💾 commit + 🚀 push → ✅ GitHub
```



## АЛЬТЕРНАТИВА!!
```text
В пример взял свою ситуацию

```

```bash

set -euo pipefail

# Папка с репозиторием препода (mfua)
TEACHER_REPO="/c/Users/dncik/Desktop/mfua"

# Папка с твоим репозиторием (PITPO)
MY_REPO="/c/Users/dncik/Desktop/PITPO"

echo "🔄 Обновление репозитория препода (mfua)..."
cd "$TEACHER_REPO"
git pull

echo "📁 Копирование файлов из mfua в PITPO (без .git)..."
rsync -a --delete --exclude='.git' "$TEACHER_REPO/" "$MY_REPO/"

echo "✅ Файлы скопированы. Подготовка коммита в PITPO..."
cd "$MY_REPO"

echo "Проверка статуса..."
git status

# Если нет изменений — выходим
if git diff --quiet; then
    echo "Нет изменений для коммита. Готово!"
    exit 0
fi

echo "Добавление всех изменений в индекс..."
git add .

# Запрашиваем сообщение для коммита
read -p "Введите сообщение коммита (или Enter для пропуска): " commit_message

if [ -z "$commit_message" ]; then
    echo "Коммит отменён."
    exit 0
fi

echo "Создание коммита: $commit_message"
git commit -m "$commit_message"

echo "🚀 Пуш в твой репозиторий (origin main)..."
git push origin main

echo "🎉 Всё готово! PITPO обновлён файлами препода и запушен."


```